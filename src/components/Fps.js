import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Fps = () => {
    return (
        <View>
            <Text></Text>
        </View>
    )
}

export default Fps

const styles = StyleSheet.create({})
